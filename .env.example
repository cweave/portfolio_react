# Optional: GitHub personal access token for higher API rate limit (5000/hr vs 60/hr).
# Create at https://github.com/settings/tokens (no scopes required for public repos).
# Copy this file to .env.local and set your token.
# REACT_APP_GITHUB_TOKEN=your_token_here
